import React from 'react';
import ReactDOM from 'react-dom';

const rootEl = document.getElementById('root');

const Sample = () => (
  <h1>sample</h1>
);

ReactDOM.render(
  <div>
    <Sample />
  </div>,
  rootEl
);
